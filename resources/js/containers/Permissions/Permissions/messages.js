/*
 * Permissions Messages
 *
 * This contains all the text for the Permissions container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.Permissions';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Welcome Guruu',
  },
});

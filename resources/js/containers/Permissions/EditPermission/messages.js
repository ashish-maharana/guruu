/*
 * Edit Permission Messages
 *
 * This contains all the text for the Permission container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.EditPermission';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'Edit Permission',
    }
});

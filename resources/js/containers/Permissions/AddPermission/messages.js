/*
 * Add Permission Messages
 *
 * This contains all the text for the Add Permission container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.AddPermission';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'Add Permission',
    },
});

/*
 * Edit Role Messages
 *
 * This contains all the text for the HomePage container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.EditRole';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'Edit Role',
    }
});

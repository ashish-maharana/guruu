/*
 * Add Role Messages
 *
 * This contains all the text for the Add Role container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.AddRole';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'Add Role',
    },
});

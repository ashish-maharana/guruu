/*
 * Roles Messages
 *
 * This contains all the text for the Roles container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.Roles';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Welcome Guruu',
  },
});

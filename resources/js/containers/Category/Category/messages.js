/*
 * category Messages
 *
 * This contains all the text for the category container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.Category';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Welcome Guruu',
  },
});

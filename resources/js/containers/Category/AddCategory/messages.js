/*
 * Add Category Messages
 *
 * This contains all the text for the Add Category container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.AddCategory';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'Add Category',
    },
});

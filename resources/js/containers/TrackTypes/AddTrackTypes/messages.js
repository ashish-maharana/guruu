/*
 * Add TrackTypes Messages
 *
 * This contains all the text for the Add TrackTypes container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.AddTrackTypes';

export default defineMessages({
    header: {
        id: `${scope}.header`,
        defaultMessage: 'Add Track',
    },
});

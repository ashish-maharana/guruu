/*
 * TrackTypes Messages
 *
 * This contains all the text for the TrackTypes container.
 */
import { defineMessages } from 'react-intl';

export const scope = 'containers.TrackTypes';

export default defineMessages({
  header: {
    id: `${scope}.header`,
    defaultMessage: 'Welcome Guruu',
  },
});
